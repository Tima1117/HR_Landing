# Папка для статических файлов

## 📁 Логотип

Поместите ваш логотип в эту папку с именем:
- `logo.png` (предпочтительно)
- или `logo.svg`

Рекомендуемые размеры:
- Минимум: 200x200px
- Оптимально: 512x512px или векторный SVG

## 🎨 У вас есть логотип в 2 цветах:

1. **logo.png** - основной цветной логотип (для светлого фона)
2. **logo-white.png** - белый логотип (для темного фона/шапки)

После добавления логотипов, обновите компоненты:
- `components/Header.tsx` - для шапки сайта
- `components/Footer.tsx` - для подвала

### Пример использования в коде:

```tsx
import Image from 'next/image'

<Image 
  src="/logo.png" 
  alt="CVortex Logo" 
  width={40} 
  height={40}
/>
```

