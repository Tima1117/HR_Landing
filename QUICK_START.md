# 🚀 Быстрый старт

## Шаг 1: Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
npm install
```

Это установит все необходимые пакеты (Next.js, React, Tailwind CSS, Framer Motion и др.)

---

## Шаг 2: Добавьте логотип (опционально)

Положите ваши логотипы в папку `public/`:
- `public/logo.png` - основной логотип
- `public/logo-white.png` - белый логотип (для темного фона)

**Если у вас нет логотипа прямо сейчас** - не проблема! Используется красивая заглушка.

---

## Шаг 3: Запуск локально

```bash
npm run dev
```

Откройте браузер и перейдите на: **http://localhost:3000**

Вы увидите ваш лендинг! 🎉

---

## Шаг 4: Проверьте все секции

На лендинге есть:

1. ✅ **Hero секция** - с призывом к действию
2. ✅ **Как работает** - визуальное описание процесса (6 шагов)
3. ✅ **Преимущества** - 6 карточек преимуществ
4. ✅ **Контакты + Форма** - форма обратной связи и контакты

Все секции адаптивны и работают на мобильных устройствах! 📱

---

## Шаг 5: Настройка отправки email (опционально)

По умолчанию форма работает, но не отправляет реальные email.

**Для настройки смотрите:** `EMAIL_SETUP.md`

Рекомендую начать с **Telegram Bot API** (самый простой вариант):

```typescript
// app/api/contact/route.ts - уже создан для вас
// Просто добавьте переменные окружения в .env.local
```

---

## Шаг 6: Деплой на Vercel

Когда все готово, следуйте инструкциям в файле: **`DEPLOY_GUIDE.md`**

Краткая версия:
```bash
# 1. Инициализируем git
git init
git add .
git commit -m "Initial commit"

# 2. Загружаем на GitHub
gh repo create cvortex-landing --public --source=. --push

# 3. Деплоим на Vercel (через веб-интерфейс или CLI)
vercel
```

---

## 🎨 Кастомизация

### Цвета
Настраиваются в `tailwind.config.js`:
```javascript
telegram: {
  blue: '#0088CC',
  light: '#64B5F6',
  dark: '#0077B5',
}
```

### Контент
- **Hero текст**: `components/Hero.tsx`
- **Преимущества**: `components/Benefits.tsx`
- **Как работает**: `components/HowItWorks.tsx`
- **Контакты**: `components/Contact.tsx` и `components/Footer.tsx`

### Мета-данные для SEO
- **Title, Description**: `app/layout.tsx`
- **Open Graph**: `app/opengraph-image.tsx`

---

## 📝 Полезные команды

```bash
# Запуск в режиме разработки
npm run dev

# Проверка кода (линтер)
npm run lint

# Сборка для production
npm run build

# Запуск production версии локально
npm run build && npm start
```

---

## 🐛 Возможные проблемы

### Ошибка "Module not found"
```bash
rm -rf node_modules package-lock.json
npm install
```

### Порт 3000 занят
```bash
# Укажите другой порт
npm run dev -- -p 3001
```

### Ошибки TypeScript
```bash
# Проверьте файл tsconfig.json
npm run build
```

---

## 📞 Контакты в проекте

Сейчас в лендинге используются ваши контакты:
- **Email**: tima_grachev@cvortex.com
- **Телефон**: +7 (916) 900 06 71
- **Telegram**: @Tima_a

Если нужно изменить, отредактируйте:
- `components/Contact.tsx` (форма и контакты)
- `components/Footer.tsx` (футер)
- `app/api/contact/route.ts` (получатель заявок)

---

## ✨ Что дальше?

1. ✅ Запустите проект локально
2. ✅ Проверьте все анимации и переходы
3. ✅ Добавьте логотип
4. ✅ Настройте email отправку
5. ✅ Задеплойте на Vercel
6. ✅ Поделитесь с командой!

---

## 🎉 Готово!

Ваш современный лендинг для CVortex готов к использованию!

**Есть вопросы?** Смотрите:
- `README.md` - общая информация
- `DEPLOY_GUIDE.md` - подробная инструкция по деплою
- `EMAIL_SETUP.md` - настройка отправки email

**Удачи с проектом!** 🚀

