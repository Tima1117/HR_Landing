# 🚀 Инструкция по деплою на Vercel

## Подготовка

1. Убедитесь, что у вас есть аккаунт на [GitHub](https://github.com)
2. Зарегистрируйтесь на [Vercel](https://vercel.com) (можно через GitHub)

---

## Шаг 1: Инициализация Git репозитория

Откройте терминал в папке проекта и выполните:

```bash
# Инициализируем git репозиторий
git init

# Добавляем все файлы
git add .

# Создаем первый коммит
git commit -m "Initial commit: CVortex landing page"
```

---

## Шаг 2: Создание репозитория на GitHub

### Вариант A: Через веб-интерфейс
1. Перейдите на https://github.com/new
2. Введите название: `cvortex-landing`
3. Нажмите "Create repository"
4. Выполните команды из раздела "…or push an existing repository from the command line":

```bash
git remote add origin https://github.com/ВАШ_USERNAME/cvortex-landing.git
git branch -M main
git push -u origin main
```

### Вариант B: Через GitHub CLI (если установлен)
```bash
gh repo create cvortex-landing --public --source=. --remote=origin --push
```

---

## Шаг 3: Деплой на Vercel

### Способ 1: Через веб-интерфейс (Рекомендуется) ⭐

1. Перейдите на https://vercel.com/new
2. Нажмите "Import Git Repository"
3. Выберите ваш репозиторий `cvortex-landing`
4. Vercel автоматически определит, что это Next.js проект
5. **Framework Preset**: Next.js (должно определиться автоматически)
6. **Root Directory**: `./` (оставьте по умолчанию)
7. **Build Command**: `npm run build` (по умолчанию)
8. **Output Directory**: `.next` (по умолчанию)

#### Переменные окружения (Environment Variables):
Если вы настроили email отправку, добавьте переменные:
- Нажмите "Environment Variables"
- Добавьте необходимые переменные (например, `RESEND_API_KEY`)

9. Нажмите **"Deploy"**

### Готово! 🎉
- Ваш сайт будет доступен по адресу: `https://cvortex-landing.vercel.app`
- Vercel покажет вам URL после деплоя

---

### Способ 2: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Выполните деплой
vercel

# Следуйте инструкциям в терминале
# При первом деплое нужно будет авторизоваться
```

---

## Шаг 4: Настройка кастомного домена (Опционально)

1. В панели Vercel перейдите в настройки проекта
2. Выберите вкладку "Domains"
3. Добавьте свой домен (например, `cvortex.com`)
4. Следуйте инструкциям по настройке DNS записей

### Если у вас есть домен:
- **Тип A**: направьте на IP Vercel (указан в настройках)
- **Или CNAME**: направьте на `cname.vercel-dns.com`

---

## Автоматические деплои 🔄

После первого деплоя:
- **Каждый push в `main`** → автоматически деплоится в production
- **Pull requests** → создается preview деплой для тестирования
- **Другие ветки** → создаются preview URL

### Workflow:
```bash
# Внесли изменения
git add .
git commit -m "Update hero section"
git push origin main

# Vercel автоматически задеплоит изменения!
```

---

## Полезные команды

```bash
# Посмотреть статус деплоя
vercel ls

# Посмотреть логи
vercel logs

# Открыть проект в браузере
vercel open

# Переключиться на production
vercel --prod
```

---

## Troubleshooting

### Ошибка: "Module not found"
```bash
# Очистите кэш и переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

### Ошибка при сборке
1. Убедитесь, что проект собирается локально:
```bash
npm run build
```
2. Если локально работает, проверьте версии Node.js в Vercel (Settings → General → Node.js Version)

### Медленная загрузка изображений
- Используйте компонент `next/image` для оптимизации
- Конвертируйте изображения в WebP формат

---

## Оптимизация после деплоя

1. **Analytics**: Включите Vercel Analytics для отслеживания посещений
2. **Speed Insights**: Включите для мониторинга производительности
3. **Web Vitals**: Отслеживайте Core Web Vitals для SEO

---

## Полезные ссылки

- [Документация Vercel](https://vercel.com/docs)
- [Next.js на Vercel](https://vercel.com/docs/frameworks/nextjs)
- [Кастомные домены](https://vercel.com/docs/concepts/projects/custom-domains)
- [Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)

---

## Что дальше?

После успешного деплоя:
1. ✅ Проверьте все ссылки и формы
2. ✅ Протестируйте на мобильных устройствах
3. ✅ Настройте Google Analytics (опционально)
4. ✅ Настройте email отправку (см. EMAIL_SETUP.md)
5. ✅ Добавьте ваш логотип в папку `public/`
6. ✅ Поделитесь ссылкой с командой!

---

**Готово!** Ваш лендинг CVortex теперь доступен в интернете! 🎉

